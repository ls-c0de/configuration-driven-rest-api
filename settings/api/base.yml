name: example-api
settings:

model:
  - tablename: users
    fields:
      - name: id
        datatype: INTEGER PRIMARY KEY NOT NULL
      - name: email
        datatype: INTEGER
  - tablename: posts
    fields:
      - name: id
        datatype: INTEGER PRIMARY KEY NOT NULL
      - name: email
        datatype: INTEGER
      - name: content
        datatype: VARCHAR(255)
endpoint:
  - description: List users
    path: /users
    method: GET
    table: users
